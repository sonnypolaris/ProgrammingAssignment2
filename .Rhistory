library("datasets")
data(iris)
iris
idx <- iris$secies == 'virginca'
idx
idx <- iris$Species == "virginica"
idx
x <- iris[idx,]
x
mean(x$Sepal.Length)
a <- tapply(mtcars$cyl, mtcars$mpg, mean)
a
tapply(mtcars$mpg, mtcars$cyl, mean)
b <-tapply(mtcars$hp, mtcars$cyl, mean)
b
b[3] - b[1]
setwd("~/Documents/repos/r-assignment-2/ProgrammingAssignment2")
ls
ls()
clear()
